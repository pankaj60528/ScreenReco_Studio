# S3 Configuration (Optional - if omitted, files are stored locally under public/uploads)
S3_BUCKET=your-bucket-name
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key

# Optional: Override public URL for links (otherwise relative paths are used)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Optional: Custom S3 endpoint/public URL for R2/MinIO
S3_ENDPOINT=https://accountid.r2.cloudflarestorage.com
S3_PUBLIC_URL=https://bucket.accountid.r2.cloudflarestorage.com
